apiVersion: lifecycle.keptn.sh/v1beta1
kind: KeptnTaskDefinition
metadata:
  name: promotion
  namespace: podtato-kubectl
spec:
  deno:
    inline:
      code: |
        console.log("Promotion Task has been executed");
