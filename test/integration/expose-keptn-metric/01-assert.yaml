apiVersion: kuttl.dev/v1beta1
kind: TestAssert
commands:
  - command: PREFIX="kuttl-test-" && NAME=$(echo $NAMESPACE | sed "s/^$PREFIX//" ) && kubectl wait --for=condition=complete job $NAME-2 -n $NAMESPACE
